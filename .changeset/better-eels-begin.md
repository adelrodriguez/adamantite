---
"adamantite": patch
---

- Bump `oxlint-tsgolint` to `0.14.2`.
- Include `oxlint-tsgolint` in the `adamantite update` dependency check so it gets updated alongside other Adamantite-managed dev dependencies.
