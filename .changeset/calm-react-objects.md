---
"adamantite": patch
---

Remove several overly strict performance and style rules from the React lint preset, including the rules that reject newly created prop values, enforce handler naming, and prohibit class component state.
