---
"adamantite": minor
---

Add `--only lint` and `--only format` to `adamantite check` and `adamantite fix` to run only the Oxlint stage or only the Oxfmt stage. Arguments after `--` go to the selected stage, and to Oxlint when both run.
