---
"adamantite": minor
---

Add `--lint` and `--format` to `adamantite check` and `adamantite fix` to run only the Oxlint step or only the Oxfmt step. Arguments after `--` go to Oxfmt with `--format`, and to Oxlint otherwise.
