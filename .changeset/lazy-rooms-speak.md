---
"adamantite": minor
---

Make `adamantite doctor` emit verifiable repair findings and prompts instead of mutating projects. Interactive runs can launch Claude Code or Codex, while `update` now only updates managed dependencies. Keep `doctor --fix` as a one-release error stub and remove the deprecated `typecheck` alias.
