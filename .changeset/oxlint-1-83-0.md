---
"adamantite": patch
---

Update the managed `oxlint` version to 1.83.0. Run `adamantite update` to pick it up; no config change is necessary. Includes React 19.3 support in `react/jsx-no-useless-fragment` and `react/no-unknown-property`.
