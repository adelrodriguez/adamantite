---
"adamantite": patch
---

Update the bundled Knip config for Knip v6 and refresh the bundled linting and formatting tool versions.

Newly generated Knip configs now use the v6 schema URLs and drop the removed `classMembers` rule.
