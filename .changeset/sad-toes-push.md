---
"adamantite": minor
---

Add `analyze` command for finding unused code with knip

Finds unused dependencies, exports, and files in your project. Supports `--fix` to auto-remove issues and `--strict` for production-focused analysis.
