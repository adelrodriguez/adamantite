---
"adamantite": patch
---

Seed the `adamantite doctor` agent handoff with the combined Markdown repair prompt. The agent now starts with every finding instead of a one-line instruction to run Doctor itself.
