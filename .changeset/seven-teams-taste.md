---
"adamantite": minor
---

Run Oxfmt as part of `adamantite check` and `adamantite fix`. Both commands now run
formatting and linting even when the first tool fails. Forwarded arguments still apply only
to Oxlint.
