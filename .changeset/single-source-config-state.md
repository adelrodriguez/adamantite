---
"adamantite": patch
---

Derive tooling config state, legacy-config warnings, and migration checks from a single detection pass so `init`, `doctor`, and `update` report consistent messages, and run `update` migrations from migration checks directly.
