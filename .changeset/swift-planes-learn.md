---
"adamantite": minor
---

Add automatic editor extension installation during setup

The `init` command now prompts to install recommended editor extensions (like oxc.oxc-vscode) automatically, improving the initial setup experience.
