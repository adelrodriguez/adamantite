/**
 * Available scripts that can be added to package.json during initialization.
 */
export type Script =
  | "check"
  | "fix"
  | "format"
  | "typecheck"
  | "check:monorepo"
  | "fix:monorepo"
  | "analyze"
